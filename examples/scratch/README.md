# Scratch / historical

`poc_openai_record_replay.py` — single-file proof-of-concept from week 1 (write-before-return + one OpenAI call). Superseded by the monorepo under `packages/`. Kept for reference only; not part of the supported product surface.
