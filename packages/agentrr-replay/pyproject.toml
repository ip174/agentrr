[project]
name = "agentrr-replay"
version = "0.1.0a1"
requires-python = ">=3.11"
dependencies = [
    "agentrr-core",
]

[tool.uv.sources]
agentrr-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agentrr_replay"]
